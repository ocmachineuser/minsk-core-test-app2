/**
 * Script with this name is normally loaded via plugin https://github.com/OpusCapita/grails-build-process/
 */
target(main: "Add plugin resource to customization area") {
    println "Just kidding..."
}

setDefaultTarget(main)
